interface:
  display_name: "Update Release Version"
  short_description: "Version releases and write changelog entries"
  default_prompt: "Use $update-release-version to classify my changes, update the root version, and prepare categorized release notes."
